Each problem's Python file includes three sections for three different step sizes. Running the code will generate the graphs for the corresponding step size.
For mac os, install these in the terminal: 
pip3 install numpy pandas matplotlib
pip3 install scipy
